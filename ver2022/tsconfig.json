{
  "include": ["src"], // js로 컴파일하고 싶은 모든 것들
  "compilerOptions": {
    "outDir": "./build", // 컴퍼일된 JS파일이 생성될 공간 설정
    "target": "ES6", // 컴파일할 JS버전을 설정
    "lib": ["ES6", "DOM"], // JS파일이 어디서 작동하는지를 런타임환경을 설정
    "strict": true
  }
}
